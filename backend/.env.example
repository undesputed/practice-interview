# Long-lived Deepgram key — stays server-side, used to mint ephemeral tokens.
DEEPGRAM_API_KEY=
# Anthropic key — used for the agent "think" provider and post-interview coaching.
ANTHROPIC_API_KEY=
# LOCAL DEV ONLY. If your Deepgram key cannot mint ephemeral tokens (403 on
# /v1/auth/grant), set this to 1 to send the long-lived key to the browser so the
# interview can run locally. NEVER set this on a deployed/EC2 server — it exposes
# the key. The proper fix for deployment is a Deepgram key WITH grant permission.
# DEEPGRAM_ALLOW_BROWSER_KEY=1

# Set to 1 to enable DeepFace emotion analysis: the live Facial screen's DeepFace
# engine and the post-interview emotion track. Requires the optional heavy install
# `pip install -r backend/requirements-emotion.txt` (TensorFlow). Leave unset to disable.
# EMOTION_ANALYSIS=1

# Face detector for the LIVE DeepFace path (the Facial screen). A real detector
# detects + eye-aligns the face before scoring, which is much more accurate than the
# raw webcam crop. Default "retinaface" (most accurate); set "opencv" for speed if the
# live read feels laggy. (The post-interview batch path always uses "skip".)
# EMOTION_DETECTOR=retinaface
