# backend/requirements-emotion.txt
# OPTIONAL — only needed to enable emotion analysis (EMOTION_ANALYSIS=1).
# Emotion uses HSEmotion (AffectNet EfficientNet) via ONNX Runtime — lighter than the
# old DeepFace/TensorFlow stack and more accurate on real webcam faces. The model
# (~30 MB) downloads on first use. Install separately from backend/requirements.txt.
hsemotion-onnx==0.3.1
opencv-python==4.10.0.84
